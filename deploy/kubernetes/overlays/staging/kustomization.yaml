"""Staging environment overlay."""

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: oryon-ai-staging

bases:
  - ../../

nameSuffix: -staging

commonLabels:
  environment: staging

# Override replicas for staging
replicas:
  - name: fastapi
    count: 2
  - name: celery-worker
    count: 2

# Use staging-specific images
images:
  - name: oryon-ai/api
    newName: oryon-ai/api
    newTag: staging
  - name: oryon-ai/worker
    newName: oryon-ai/worker
    newTag: staging

# Staging-specific secrets (should be created separately)
secretGenerator:
  - name: oryon-secrets
    behavior: replace
    literals:
      - POSTGRES_PASSWORD=${STAGING_DB_PASSWORD}
      - FLASK_SECRET_KEY=${STAGING_SECRET_KEY}
